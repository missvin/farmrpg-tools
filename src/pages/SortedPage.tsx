import { PageIntro } from '../components/PageIntro';

export function SortedPage() {
  return (
    <PageIntro
      title="Sorted Mastery Progress"
      description="Browse your mastery progress sorted by the categories and filters we add later."
    />
  );
}
